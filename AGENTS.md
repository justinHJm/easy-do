# Expo 버전 주의

Expo는 버전마다 API와 사용법이 달라질 수 있다.

코드를 작성하거나 Expo 관련 기능을 구현하기 전에 반드시 아래의 정확한 버전 문서를 우선 확인한다.

https://docs.expo.dev/versions/v57.0.0/

현재 프로젝트는 Expo SDK 57을 기준으로 한다.

최신 버전 문서의 사용법을 SDK 57 프로젝트에 그대로 적용하지 않는다.
공식 Expo 57 문서와 현재 프로젝트의 실제 dependency 버전을 우선한다.


# easy-do Codex 작업 규칙

## 프로젝트 정보

- Android 전용 Todo 앱 `easy-do`
- React Native + Expo + TypeScript 사용
- 현재 Expo SDK 57 사용
- 사용자가 직접 사용하면서 점진적으로 기능을 추가하는 프로젝트
- 복잡한 구조보다 이해하기 쉽고 유지보수하기 쉬운 코드를 우선한다.


## 개발 기본 원칙

- 변경 사항은 작고 명확하게 유지한다.
- 요청받지 않은 기능은 수정하지 않는다.
- 현재 정상 동작하는 기능을 최대한 보존한다.
- 관련 없는 파일을 불필요하게 수정하지 않는다.
- 새 dependency(외부 패키지)는 명확한 필요가 있을 때만 추가한다.
- 새 dependency가 필요한 경우 추가하기 전에 왜 필요한지 설명한다.
- 기존 React Native / Expo 기능으로 구현 가능한 경우 새 dependency를 추가하지 않는다.
- 지나친 abstraction(추상화)이나 불필요한 파일 분리는 피한다.
- 초보자가 나중에 코드를 다시 읽어도 구조를 이해할 수 있도록 작성한다.
- 한 번에 대규모 리팩터링하지 않는다.
- 기능 추가와 리팩터링은 가능하면 별도의 작업으로 분리한다.


## UI / UX 기본 규칙

- 기본 배경은 흰색을 사용한다.
- 메인 강조 색상은 초록색 계열을 사용한다.
- 전체적인 디자인은 깔끔하고 단순하게 유지한다.
- 한 화면에 불필요한 장식이나 요소를 과하게 배치하지 않는다.
- Todo 목록을 최대한 많이 볼 수 있도록 화면 공간을 효율적으로 사용한다.
- Todo 체크박스는 오른쪽에 배치한다.
- 완료된 Todo는 회색 처리하고 제목에 취소선을 표시한다.
- 우선순위는 높음 / 보통 / 낮음으로 구분한다.
- 기본 우선순위는 `보통`이다.
- 사용자가 빠르게 Todo를 추가하고 완료할 수 있는 흐름을 우선한다.
- ADHD 친화적인 방향을 고려하되, 관련 고급 기능은 사용자가 명시적으로 요청하기 전까지 임의로 추가하지 않는다.


## 캐릭터 사용 규칙

- easy-do 캐릭터는 앱의 보조적인 피드백 요소로 사용한다.
- 캐릭터가 Todo 사용성을 방해할 정도로 큰 공간을 차지하지 않도록 한다.
- 캐릭터 이미지, 말풍선 이미지, 실제 대사 텍스트는 가능한 한 분리해서 관리한다.
- 랜덤 환영 대사나 완료 대사는 코드의 데이터 배열로 관리한다.
- 캐릭터 이미지 자체에 고정 문구를 넣지 않는다.
- 캐릭터의 성장, 보상, 펫 시스템은 사용자가 요청하기 전까지 구현하지 않는다.


## TypeScript 및 코드 구조

- TypeScript 타입을 가능한 명확하게 정의한다.
- `any` 사용은 가능한 피한다.
- 상태(state)의 원본 데이터를 여러 곳에 중복해서 저장하지 않는다.
- 계산 가능한 값은 가능한 원본 상태에서 계산한다.
- Context, Hook, Component를 역할에 맞게 사용하되 구조를 지나치게 복잡하게 만들지 않는다.
- 한 파일이 지나치게 커질 때만 적절하게 분리한다.
- 변수명과 함수명은 역할을 이해할 수 있게 명확하게 작성한다.


## 한국어 주석 규칙

이 프로젝트는 사용자의 프로그래밍 학습에도 사용된다.

따라서 초보자가 이해하기 어려운 핵심 코드에는 한국어 설명 주석을 작성한다.

### 주석을 작성해야 하는 경우

다음과 같은 부분에는 필요한 경우 한국어 주석을 작성한다.

- React state 관리
- `useState`
- `useEffect`
- Context / Provider
- Custom Hook
- Todo 추가 / 수정 / 삭제 로직
- Todo 완료 상태 변경
- 우선순위 정렬
- 완료율 계산
- 비동기 처리
- AsyncStorage 저장 및 불러오기
- 서버 통신
- 예외 처리
- 랜덤 캐릭터 / 랜덤 대사 선택
- 앱 시작 시 초기화 과정
- 이해하기 어려운 조건문이나 데이터 변환
- Android 네이티브 기능
- Android Widget 관련 코드

### 좋은 주석의 기준

주석은 단순히 코드를 한국어로 번역하지 않는다.

가능하면 다음을 설명한다.

- 왜 이 코드가 필요한지
- 왜 이 방법을 사용했는지
- 다른 코드와 어떤 관계가 있는지
- 수정할 때 무엇을 조심해야 하는지

예:

```ts
// Todo 원본 배열을 따로 복제하지 않고,
// 화면에 표시할 때만 우선순위 기준으로 정렬한다.
// 이렇게 하면 원본 상태와 화면용 상태가 서로 어긋나는 문제를 줄일 수 있다.
```

# Manager와 멀티 에이전트 작업

이 프로젝트에서는 Main Codex가 Manager다. 아래 조건에 맞는 subagent(작업을 나눠 맡는 보조 에이전트) 사용을 명시적으로 허용한다.
작업 시작 시 요구사항, 관련 파일, 현재 Git 상태와 기존 사용자 변경을 확인하고 복잡도를 짧게 판단한다.

- 작은 작업: Manager가 직접 구현하거나 Worker 1개. 불필요한 Explorer를 생성하지 않는다.
- 중간 작업: 독립된 파일 영역으로 나눌 수 있을 때 Worker 2개까지. 의존성이 있으면 순차 처리한다.
- 큰 작업: UI·상태 로직·저장·Android처럼 소유 파일을 분리할 수 있을 때 최대 Worker 3개. 수는 작업별로 결정한다.
- 프로젝트 설정의 자식 동시 상한은 3개이며 Manager는 제외한다. 실제 환경의 더 낮은 제한을 우선한다. Explorer/Reviewer도 같은 슬롯을 사용한다.
- 생성·배정·조율은 Manager만 한다. 자식의 재위임은 금지한다. 끝난 자식은 닫거나 재사용하고, 슬롯 해제를 지원하지 않으면 순차 진행한다.
- Explorer는 필요한 코드 위치·의존 관계를 읽기 위주로 조사할 때만 쓴다.
- Worker마다 요구사항, 소유 파일, 제외 범위, 공유 인터페이스, Done when(완료 기준)을 전달한다. 같은 파일 동시 수정을 피한다.
- 조사 결과와 파일 위치를 공유해 중복 탐색을 줄인다. 전체 대화 복제보다 필요한 배경만 전달한다.
- Worker는 변경 파일·핵심 변경·검증·남은 문제를 보고한다. 버전과 CHANGELOG는 Manager만 수정한다.

역할 정의는 `.codex/agents/{explorer,worker,reviewer}.toml`이다.
실제 생성 도구가 역할 선택을 지원하면 해당 custom agent(역할별 설정)를 사용한다.
현재 도구에 역할 선택 인자가 없으면 존재하지 않는 인자를 만들지 말고, Manager가 역할 파일을 읽어 해당 지침을 작업 메시지에 전달한다.
이 대체 경로에서는 TOML 권한 설정이 자동 적용됐다고 주장하지 않는다. 현재 세션 권한과 도구 제한이 우선하며 읽기 전용은 행동 규칙으로도 지킨다.

## Reviewer 검증 관문

구현이 끝나면 별도의 Reviewer 1개에 이번 요청·변경 범위·시작 시점 Git 상태·검증 결과를 전달한다.
Reviewer는 diff와 변경 파일부터 읽고 필요할 때만 호출부를 더 조사한다. untracked(아직 Git이 추적하지 않는) 신규 파일도 확인한다.
작은 작업도 구현용 자식을 여러 개 만들지 않고 최종 Reviewer만 추가할 수 있다.
조사만 하는 요청에는 구현 검증·버전 증가를 강제하지 않는다.

검증 순서: 요구사항 → 실제 오류 → 상태/저장 → Expo SDK57 호환 → TypeScript → 회귀 → 테스트 → Android 동작 가능성.
검증 절차는 `.agents/skills/verify/SKILL.md`를 사용한다.
Reviewer는 코드를 직접 고치거나 버전을 올리지 않는다. 읽기 전용에서 불가능한 검사는 Manager가 실행하고 증거를 전달한다.
결함이 있으면 FAIL과 파일/줄·원인·수정 방향을 보고한다. 필요한 검증을 할 수 없고 대체 증거도 없으면 BLOCKED로 구분한다.
Manager는 해당 담당 Worker만 재투입한다. Manager가 구현한 부분은 Manager가 고친다. 같은 Reviewer가 해당 변경을 재검증한다.
결함이 없으면 PASS와 검증 범위·남은 기기 확인 항목을 보고한다. 실행하지 않은 검사를 통과했다고 쓰지 않는다.
Reviewer 도구가 없으면 제한을 보고하고 자기 검토를 독립 Reviewer PASS라고 부르지 않는다.

## 자동 버전과 CHANGELOG

Semantic Versioning(변경 의미에 따라 MAJOR.MINOR.PATCH를 올리는 규칙)을 작업 단위로 적용한다.
기준은 `app.json`의 `expo.version`이며 `package.json`, `package-lock.json`의 프로젝트 버전도 일치시킨다.
실제 앱 동작/UI/UX/기능에 사용자가 체감할 수 있는 변경이 완료되고 Reviewer PASS를 받은 경우에만 Manager가 PATCH 또는 MINOR를 판단한다.
하나의 사용자 요청 또는 하나의 논리적인 작업 묶음에 대해 버전 증가는 최대 한 번이다. 증가 시 CHANGELOG를 함께 갱신한다.

- PATCH: 사용자가 체감하는 버그 수정, 작은 UI/UX 개선. 내부 코드 개선·리팩터링은 실제 사용자 체감 변화가 있는 경우에만 판단한다.
- MINOR: 새 사용자 기능, 의미 있는 기능 변경, 앱 개발 마일스톤.
- MAJOR/1.0.0: 사용자 승인 없이는 금지. 현재는 0.x.x를 유지한다.
- 단순 조사, 주석/문서 수정, Codex 설정 변경, 테스트만 수행한 경우에는 앱 버전을 증가시키지 않는다. 필요한 기록만 CHANGELOG의 Unreleased에 남긴다.
- 작업 도중의 여러 수정, Reviewer 재작업·재검증, 후속 메시지로 이어지는 같은 작업 묶음은 추가 버전 증가의 이유가 아니다. commit 개수로 버전을 판단하지 않는다.
- Manager는 작업 시작 버전과 이번 작업의 증가 여부를 확인한다. 이미 한 번 증가했다면 재작업 후에도 버전은 유지하고 같은 CHANGELOG 항목을 보완한다. 대화 재개 시에도 기존 보고와 CHANGELOG를 확인해 중복 증가를 피한다.
- `node scripts/manage-version.cjs check`로 일치를 확인한다. `patch|minor --reason "설명"`은 미리보기이고 `--apply`를 붙였을 때만 갱신한다.
- 버전·CHANGELOG 변경 후 Reviewer가 그 마지막 diff를 짧게 재확인한다.
- `android.versionCode`는 앱 표시 버전과 별도인 빌드 번호다. 현재 명시 설정이 없으므로 이번 구성에서는 추가하지 않는다.
- APK/AAB 작업 때 실제 EAS의 remote/local 버전 소스를 확인하고 한 방식으로만 관리한다. 빌드 번호를 임의로 재설정·대폭 증가시키지 않는다.

## Git 안전 정책

사용자가 해당 작업을 명시적으로 요청하지 않으면 commit, push, tag, GitHub Release 생성, 원격 브랜치 변경/삭제, force push를 실행하지 않는다.
구현·검증·버전 증가·릴리스 준비 요청은 위 Git/배포 동작의 허가가 아니다. commit 요청은 push 허가가 아니다.
Worker/Explorer/Reviewer는 위 동작을 하지 않는다. 명시 요청을 받은 Manager만 요청 범위 내에서 실행한다.
기존 staged(커밋 대상으로 선택된) 변경도 사용자의 작업이다. 임의로 stage/unstage/reset하거나 `git add .`로 전체를 포함하지 않는다.
완료 보고에는 이번 변경 파일, 이전→현재 버전 또는 유지 이유, Reviewer 결과, 실행한 검사와 제한, 미커밋 여부를 포함한다.
규칙은 에이전트 행동 지침이며 Git 프로그램 자체를 잠그는 장치가 아니다.

## 프로젝트 Skills

공식 프로젝트 경로 `.agents/skills/`에 반복 작업 지침을 둔다.
`$verify`, `$expo-start`, `$build-apk`, `$release`, `$commit`으로 지정하거나 평소 문장으로 요청한다.
스킬 사용은 그 안의 외부 업로드·Git 쓰기 권한을 자동으로 부여하지 않는다.
Windows PowerShell에서는 `npx.cmd`, `npm.cmd`를 사용할 수 있다. 설치되지 않은 검사 도구를 검사 명령으로 자동 설치하지 않는다.
운영 방법과 확인된 환경은 `docs/CODEX.md`를 참고한다.
